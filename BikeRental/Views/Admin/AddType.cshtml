@using BikeRental.Resources.App_GlobalResources
@model BikeRental.ViewModel.AddTypeViewModel
@{
    ViewBag.Title = Resource.AddType;
}

<header id="head" class="secondary"></header>
<div class="container">
    <div class="row">
        <article class="col-xs-12 maincontent">
            <header class="page-header">
                <h1 class="page-title">@Resource.AddType</h1>
            </header>
            <div class="col-md-6 col-md-offset-3 col-sm-8 col-sm-offset-2">
                <div class="panel panel-default">
                    <div class="panel-body">
                        @using (Html.BeginForm("AddType", "Admin", null, FormMethod.Post, new { enctype = "multipart/form-data" }))
                        {
                            @Html.AntiForgeryToken()
                            <div class="top-margin">
                                @Html.EditorFor(model => model.NameType, new { htmlAttributes = new { @class = "form-control", @placeholder = "Email" } })
                                <p>@Html.ValidationMessageFor(model => model.NameType, "", new { @class = "text-danger" })</p>
                            </div>
                            
                            <div class="row">
                                <input type="submit" value="@Resource.AddType" class="btn btn-action btn-lg" style="width: 100%">
                            </div>
                        }
                    </div>
                </div>
            </div>
        </article>
    </div>
</div>



